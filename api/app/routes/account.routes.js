 const item = require("../controllers/account.controller.js");

  let router = require("express").Router();
  // Create a new Items
  // router.post("/create", item.create);
  //
  // // // Create a new Items History TEST
  // // router.post("/createhistory", item.createH);
  //
  // // Search items
  // router.get("/search/", item.search);
  //
  // // Retrieve all Items
  // router.get("/", item.findAll);
  //
  // // Retrieve a single Items with id
  // router.get("/find/:id", item.findOne);
  //
  //
  // // Update a Items with id
  // router.put("/update/", item.update);
  //
  // // // Delete a Items with id
  // router.delete("/delete/:id", item.delete);



  module.exports = router;
